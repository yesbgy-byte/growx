---
description: Push changes to GitHub to trigger Vercel deployment
---

1. Stage all changes
```bash
git add .
```

2. Commit changes (User should provide a message, or use a default one)
```bash
git commit -m "Update project"
```

3. Push to remote
```bash
git push origin main
```
